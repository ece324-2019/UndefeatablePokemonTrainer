import torch
import torch.optim as optim
import torch.nn as nn
import torch.nn.functional as F
import matplotlib.pyplot as plt

# Load the data



#def train(args, model, criterion, optimizer, train_iter, val_iter)
